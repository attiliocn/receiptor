{
  "fixtures": [
    {
      "id": "paraiso_baseline",
      "path": "tests/fixtures/pdfs/paraiso_baseline.pdf",
      "type": "pdf",
      "expected": {
        "status": "pass",
        "minimum_items": 1
      },
      "notes": "Baseline passing supermarket receipt fixture"
    },
    {
      "id": "antonelli_variant",
      "path": "tests/fixtures/pdfs/antonelli_variant.pdf",
      "type": "pdf",
      "expected": {
        "status": "pass",
        "minimum_items": 1
      },
      "notes": "Different layout variant — previously a known issue, now passes"
    },
    {
      "id": "mixed_case_item_text",
      "path": "tests/fixtures/text/mixed_case_item.txt",
      "type": "text",
      "expected": {
        "status": "known_issue",
        "minimum_items": 1
      },
      "notes": "Current regex expects item names to start in uppercase and may miss this"
    },
    {
      "id": "has_comma_prod_name",
      "path": "tests/fixtures/pdfs/has_comma_prod_name.pdf",
      "type": "pdf",
      "expected": {
        "status": "known_issue",
        "minimum_items": 1
      },
      "notes": "The parser extract the fields as expected, but product names that have commas will cause a error when splitting the columns in Excel because the comma is not escaped"
    }
  ]
}
